""" Match two datasets on terms and locations

We're going to need to do some things because a foreign key doesn't
exist between two tables.
"""

# lowercase everything
# remove punctuation
# replace spaces with underscores

# full outer join if it's available

# assess accuracy with frequencies

# rewrite with unique ids from _key.csv
